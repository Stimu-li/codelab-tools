{
  "name": "codelab-elements",
  "version": "1.0.1",
  "description": "Custom elements for codelabs",
  "main": "index.js",
  "scripts": {
    "clean": "node_modules/.bin/bazel clean",
    "build": "node_modules/.bin/bazel build npm_dist && unzip -o bazel-bin/npm_dist.zip -d codelab-elements/build",
    "test": "node_modules/.bin/bazel test --test_output=all codelab-elements/demo:hello_test",
    "pub": "npm run clean && npm run build && cd bazel-genfiles && unzip npm_dist.zip -d npm_dist && npm publish npm_dist",
    "demo": "cd claat && go build -o bin/claat.exe && cd ..\\sample-codelabs && ..\\claat\\bin\\claat.exe export -prefix ../../codelab-elements/build test-all-features.md"
  },
  "author": "Google (original), Stimu-li (fork maintainer)",
  "repository": {
    "type": "git",
    "url": "https://github.com/Stimu-li/codelab-tools.git"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@bazel/bazelisk": "^1.23.1"
  },
  "dependencies": {
    "@bazel/bazel": "^2.1.0"
  }
}
